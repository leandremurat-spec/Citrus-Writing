/** The `@panel` slot for `/buffer`. Nothing here — same reasoning as `@panel/codex/page.tsx` —
    so `WorkspaceShell` drops the Command Centre column and the Buffer board gets that width. */
export default function BufferPanelSlot() {
  return null;
}
